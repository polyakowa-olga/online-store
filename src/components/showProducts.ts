// import { IProducts } from "./item/item";

// export function showProducts(arrProducts: IProducts[], arrId: number[]): void {

//   arrId.forEach((el) => {
//     const product = arrProducts.filter(elem => elem.id === el);
   
//     showProduct()
//   });

// }

// function createElement(productData: IProducts, tag: string, content: string, path: string, ...classes: string[]): HTMLElement {

//     const element = document.createElement(tag);
//     element.innerText = content;
//     classes.forEach((el) => {
//         element.classList.add(el);
//       });
//     //if(tag === 'img') {element.srs = path};
//     return element;
//   }

// function showProduct(productElem: IProducts, place: string): void {
//     for (const key in productElem) {
//         if (Object.prototype.hasOwnProperty.call(productElem, key)) {
//             const element = object[key];
            
//         }
//     }



//   }

